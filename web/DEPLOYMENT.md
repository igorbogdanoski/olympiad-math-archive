# 🚀 Водич за Објавување на Веб Архивата

Овој проект е изграден со **Astro**, што го прави екстремно брз и лесен за хостирање. Еве како да го поставите вашиот сајт во живо.

## 1. Подготовка (Локално)
Пред да го објавите сајтот, осигурајте се дека ги имате инсталирано сите зависности:
```bash
cd web
npm install
```

За да го тестирате сајтот локално, пуштете ја наредбата:
```bash
npm run dev
```
Посетете ја адресата `http://localhost:4321` за да ги видите промените.

## 2. Објавување на Vercel (Најбрза опција)
**Vercel** е идеална платформа за Astro проекти.

1.  Поставете го целото репозиториум на **GitHub**.
2.  Најавете се на [Vercel](https://vercel.com) со вашиот GitHub профил.
3.  Кликнете на **"Add New"** -> **"Project"**.
4.  Изберете го вашето репозиториум.
5.  **ВАЖНО:** Во делот "Root Directory" изберете ја папката `web`.
6.  Кликнете **Deploy**.

Vercel автоматски ќе го препознае Astro и ќе го изгради сајтот. Секој нов `git push` автоматски ќе го ажурира сајтот.

## 3. Објавување на GitHub Pages
Ако сакате да го користите GitHub Pages:

1.  Во `web/astro.config.mjs` додадете ја вашата адреса:
    ```javascript
    export default defineConfig({
      site: 'https://vashiot-username.github.io',
      base: '/olympiad-math-archive', // името на репозиториумот
      integrations: [tailwind()],
    });
    ```
2.  Користете ја наредбата `npm run build` за да генерирате статични фајлови во `dist/` папката.

## 4. Како се ажурира содржината?
Вашиот нов работен тек е:
1.  **Додадете задача** преку `tools/process_olympiad.py`.
2.  **Направете Git Commit & Push**:
    ```bash
    git add .
    git commit -m "Додадена нова задача: [име]"
    git push origin main
    ```
3.  **Готово!** Платформата (Vercel/GitHub Pages) сама ќе ја препознае промената и ќе ја објави новата задача во рок од 1 минута.

---
**Совет од Експерт:** Користете го Vercel за почеток бидејќи нуди бесплатни SSL сертификати (HTTPS) и е многу поедноставен за конфигурација со Astro.
