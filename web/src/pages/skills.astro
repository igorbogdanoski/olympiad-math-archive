---
import Layout from '../layouts/Layout.astro';
import problems from '../data/problems.json';

// Екстракција на сите уникатни вештини од задачите
const allSkills = [...new Set(problems.flatMap(p => p.meta.related_skills || []))].sort();

// Групирање на задачи по вештина
const skillStats = allSkills.map(skill => {
  const count = problems.filter(p => p.meta.related_skills?.includes(skill)).length;
  return { name: skill, count };
});
---

<Layout title="Математички Вештини | МатАрхива">
	<div class="max-w-6xl mx-auto">
		<header class="mb-16 text-center">
			<h1 class="text-5xl font-black mb-6 bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-emerald-500">
				Мајсторство на Методи
			</h1>
			<p class="text-xl text-slate-500 max-w-2xl mx-auto italic">
				"Најмоќното оружје не е теоремата, туку начинот на кој ја применувате."
			</p>
		</header>

		<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
			{skillStats.map(skill => (
				<div class="group bg-white dark:bg-slate-900 rounded-[2rem] p-8 border border-slate-100 dark:border-slate-800 shadow-sm hover:shadow-xl transition-all hover:-translate-y-2">
					<div class="w-14 h-14 bg-blue-50 dark:bg-blue-900/20 rounded-2xl flex items-center justify-center text-2xl mb-6 group-hover:scale-110 transition-transform">
						🧠
					</div>
					<h3 class="text-2xl font-bold mb-3 capitalize text-slate-900 dark:text-white">
						{skill.name.replace(/_/g, ' ')}
					</h3>
					<p class="text-slate-500 dark:text-slate-400 text-sm mb-6 line-clamp-2">
						Научете како да ја примените оваа стратегија во натпреварувачки задачи.
					</p>
					<div class="flex items-center justify-between">
						<span class="text-xs font-black uppercase tracking-widest text-blue-600">
							{skill.count} Задачи
						</span>
						<a href={`/skills/${skill.name}`} class="text-sm font-bold flex items-center gap-1 group-hover:gap-2 transition-all text-slate-900 dark:text-slate-100">
							Истражи <span class="text-blue-500">→</span>
						</a>
					</div>
				</div>
			))}
		</div>

		{allSkills.length === 0 && (
			<div class="text-center py-20 bg-slate-50 rounded-[3rem] border border-dashed border-slate-200">
				<p class="text-slate-400 font-medium italic">Секцијата се пополнува со додавање на тагови во задачите...</p>
			</div>
		)}
	</div>
</Layout>
