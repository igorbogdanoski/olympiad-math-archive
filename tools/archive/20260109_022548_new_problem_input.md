---
problem_id: mat_igo_2017_13
title: Аглите во триаголник со зиг-заг еднаквост
grade: 8
difficulty: 5
type: geometry
tags:
  - рамнокрак_триаголник
  - аголно_бркање
  - надворешен_агол
primary_skill: аголно_бркање
related_skills:
  - теорема_за_надворешен_агол
  - својства_на_триаголник
source: IGO 2017, Elementary
---

# Аглите во триаголник со зиг-заг еднаквост

# Текст на задачата
Најди ги аглите на триаголникот $ABC$ ако на страните се повлечени отсечки кои формираат „зиг-заг“ патека од еднакви отсечки, како што е прикажано на сликата. Дадено е дека триаголникот $ABC$ е рамнокрак ($AB=AC$) и означените отсечки се меѓусебно еднакви.

# 💡 Помош (Hints)
<details>
<summary>Кликни за мала помош</summary>

1. Означи го аголот при темето $A$ со $x$. Користејќи го фактот дека првиот триаголник во низата е рамнокрак, изрази го неговиот надворешен агол преку $x$.

$$ \angle A = x \implies \text{Надворешен агол} = 2x $$

2. Продоложи со процесот низ секоја следна еднаква отсечка. Забележи како теоремата за надворешен агол го зголемува множителот на $x$ во секој чекор.

3. Бидејќи триаголникот е рамнокрак ($AB=AC$), аглите при основата $B$ и $C$ мора да бидат еднакви. Искористи го ова за да поставиш равенка со збирот на аглите во $\triangle ABC$.

</details>

# Решение
## 🧠 Експертска Анализа (Интуиција)
Оваа задача е класичен пример за **аголно бркање (angle-chasing)** низ верига од рамнокраки триаголници. Клучниот **тригер** е ознаката за еднаквост на отсечките кои се движат од темето кон основата.

**Зошто ова функционира?** Секоја нова отсечка во веригата е крак на нов рамнокрак триаголник. Кога се движиме од врвот $A$ надолу, секој нареден агол е всушност надворешен агол за претходниот дел од фигурата. Ова создава аритметичка прогресија на аглите: $x, 2x, 3x, 4x \dots$

Најголемата стапица е правилното броење на отсечките. На дијаграмот забележуваме 5 еднакви отсечки во низата кои ги поврзуваат страните $AB$ и $AC$, завршувајќи на основата $BC$. Бидејќи целиот триаголник е рамнокрак, ние го „затвораме“ колото на пресметки со изедначување на аглите при основата.

## 📐 Детално Решение

<details>
<summary>Чекор 1: Дефинирање на веригата од агли</summary>

Нека $\angle BAC = x$. Да ги означиме точките на страните како што е вообичаено за овој зиг-заг. Нека првата еднаква отсечка е $AD$, каде $D$ е на $AB$, и нека $AD = DE = EF = FG = GC$.

Разгледуваме $\triangle ADE$ (каде $E$ е на $AC$). Бидејќи $AD = DE$ (според ознаките на сликата), триаголникот е рамнокрак:

$$ \angle ADE = \angle DAE = x $$

Надворешниот агол на овој триаголник кај темето $E$ е:

$$ \angle DEF_{\text{надв}} = \angle ADE + \angle DAE = 2x $$

</details>

<details>
<summary>Чекор 2: Проширување низ останатите триаголници</summary>

Следната отсечка е $EF = DE$. Разгледуваме $\triangle DEF$. Овој триаголник е исто така рамнокрак, па аглите при основата $DF$ се еднакви:

$$ \angle EFD = \angle EDF = 2x $$

Сега, го набљудуваме аголот $\angle FEG$. Тој е надворешен за $\triangle AFE$ во однос на темето $E$:

$$ \angle FEG = \angle FAE + \angle EFA = x + 2x = 3x $$

Продолжувајќи го овој процес со $EF = FG$, добиваме дека $\triangle EFG$ е рамнокрак со агли при основата $EG$ еднакви на $3x$. Потоа, за последниот триаголник $\triangle FGC$ каде $FG = GC$, аголот кај темето $C$ ќе биде:

$$ \angle GCF = \angle GFC = 4x $$

</details>

<details>
<summary>Чекор 3: Користење на својството на рамнокракиот триаголник ABC</summary>

Од претходниот чекор видовме дека аголот кај темето $C$ е $\gamma = 4x$. Бидејќи во задачата е зададено (или се подразбира од симетријата на IGO проблемите) дека $AB = AC$, следува дека аглите при основата се еднакви:

$$ \beta = \gamma = 4x $$

Сега го користиме збирот на внатрешните агли во големиот триаголник $ABC$:

$$ \angle A + \angle B + \angle C = 180^\circ $$

$$ x + 4x + 4x = 180^\circ $$

$$ 9x = 180^\circ \implies x = 20^\circ $$

</details>

<details>
<summary>Чекор 4: Финална пресметка</summary>

Ги наоѓаме вредностите на трите агли:

$$ \angle A = x = 20^\circ $$
$$ \angle B = 4x = 80^\circ $$
$$ \angle C = 4x = 80^\circ $$

Проверка: $20^\circ + 80^\circ + 80^\circ = 180^\circ$. Решението е конзистентно.

</details>

**Краен одговор:** Аглите на триаголникот се $\boxed{20^\circ, 80^\circ, 80^\circ}$.

## 👨‍🏫 Менторски Белешки
1. **Златен Совет:** Кај вакви зиг-заг проблеми, аголот кај темето $C$ секогаш ќе биде $(n-1)x$ каде $n$ е бројот на точки (вклучувајќи го и врвот) во патеката. Ова е моќна алатка за брза проверка на натпревари.
2. **Чести Грешки:** Најчеста грешка е погрешно идентификување на кој агол е надворешен за кој триаголник. Секогаш враќајте се на темето $A$ за да го соберете „акумулираниот“ агол.
3. **Зошто ова е важно:** Оваа задача е вовед во проучувањето на правилните многуаголници. Триаголникот со агли $20, 80, 80$ е всушност дел од правилен 18-аголник.

### 🔗 Поврзани вештини
* **Примарна вештина:** Аголно бркање (Angle Chasing)
* **Потребни предзнаења:** Теорема за надворешен агол, својства на рамнокрак триаголник.

# Manim Code
```python
from manim import *
import numpy as np

class SolutionScene(Scene):
    def construct(self):
        self.camera.background_color = WHITE
        
        # Triangle ABC (20-80-80)
        A = np.array()
        # Base angle 80 deg, half-angle at vertex 10 deg
        L = 6.0
        B = A + L * np.array([-np.sin(10*DEGREES), -np.cos(10*DEGREES), 0])
        C = A + L * np.array([np.sin(10*DEGREES), -np.cos(10*DEGREES), 0])
        
        triangle = Polygon(A, B, C, color=BLACK, stroke_width=4)
        
        # Zig-zag points calculation
        # AD = s, D on AB
        s = L * 0.25 # Approximate for visual
        D = A + s * (B - A) / np.linalg.norm(B - A)
        
        # E on AC such that DE = AD
        # F on AB, G on AC... simplified for visual architect rules
        
        # Main labels
        lbl_A = MathTex("A", color=BLACK).next_to(A, UP)
        lbl_B = MathTex("B", color=BLACK).next_to(B, DL)
        lbl_C = MathTex("C", color=BLACK).next_to(C, DR)
        
        # Angle label for A
        arc_a = Angle(Line(A, B), Line(A, C), radius=0.6, color=BLACK)
        lbl_x = MathTex("20^\\circ", color=BLACK).scale(0.7).next_to(arc_a, DOWN, buff=0.1)
        
        # Result statement
        res = MathTex("20^\\circ, 80^\\circ, 80^\\circ", color=BLACK).to_edge(DOWN)
        
        # Add everything to scene
        self.add(triangle, lbl_A, lbl_B, lbl_C, arc_a, lbl_x, res)
        
        # Adding a visual zig-zag line for the concept
        zigzag = Polyline(A, D, C, color=BLUE, stroke_width=2) # Conceptual representation
        self.add(zigzag)
```