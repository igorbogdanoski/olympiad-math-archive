---
problem_id: sigma_137_y4_p1855
title: Логаритамска неравенка со телескопска сума
grade: 12
difficulty: 6
type: algebra
tags:
  - логаритми
  - неравенки
  - телескопски_суми
  - квадратни_неравенки
primary_skill: логаритамски_трансформации
related_skills:
  - телескопско_сумирање
  - анализа_на_домен
source: Сигма 137
---

# Логаритамска неравенка со телескопска сума

# Текст на задачата

Во множеството на реалните броеви реши ја неравенката:

$$\frac{1}{\log_x 2 \cdot \log_x 4} + \frac{1}{\log_x 4 \cdot \log_x 8} + \dots + \frac{1}{\log_x 2024 \cdot \log_x 2025} \le 2024 \cdot \log_2 x$$

# 💡 Помош (Hints)
<details>
<summary>Кликни за мала помош</summary>

1. Прво одреди го дефинициониот домен. Каков мора да биде $x$ за логаритмите да бидат дефинирани во именителот и како основа?

2. Искористи го својството за промена на основата $\log_a b = \frac{1}{\log_b a}$ и својството $\log_a b^n = n \log_a b$. Забележи дека сите броеви во логаритмите (2, 4, 8...) се степени на бројот 2.

$$ \log_x 2^n = n \cdot \log_x 2 = \frac{n}{\log_2 x} $$

3. Кога ќе ги замениш овие изрази во сумата, ќе добиеш низа од дропки кои формираат телескопска сума.

$$ \frac{1}{k(k+1)} = \frac{1}{k} - \frac{1}{k+1} $$

</details>

# Решение
## 🧠 Експертска Анализа (Интуиција)

Оваа задача на прв поглед изгледа застрашувачки поради должината на изразот, но таа крие една од најубавите структури во олимписката алгебра: **телескопското сумирање**. 

Клучниот **тригер** е во основите и аргументите на логаритмите. Забележуваме дека сите аргументи ($2, 4, 8 \dots$) се од обликот $2^k$. Ова силно сугерира дека треба да ги префрлиме сите логаритми во основа 2. Зошто? Затоа што $\log_2 2^k$ е едноставно цел број $k$.

Втората важна работа е да се препознае дека по трансформацијата, променливата $x$ ќе се појави како $(\log_2 x)^2$ во секој член на сумата. Ова ни овозможува да ја извлечеме променливата пред заграда и да се фокусираме на бројната сума. Ако брутално се обидеме да пресметуваме без оваа трансформација, ќе заглавиме во невозможни алгебарски изрази.

## 📐 Детално Решение

<details>
<summary>Чекор 1: Одредување на домен и трансформација на членовите</summary>

Од дефиницијата на логаритамска функција, основата $x$ мора да биде позитивна и различна од 1. Исто така, бидејќи логаритмите се во именител, тие не смеат да бидат нула, што веќе е покриено со $x \neq 1$. Значи, доменот е:

$$x > 0 \quad \text{и} \quad x \ne 1$$

Сега, да го разгледаме општиот член на сумата. Користејќи ги својствата $\log_x 2^k = k \log_x 2 = \frac{k}{\log_2 x}$, имаме:

$$\frac{1}{\log_x 2^k \cdot \log_x 2^{k+1}} = \frac{1}{\frac{k}{\log_2 x} \cdot \frac{k+1}{\log_2 x}} = \frac{(\log_2 x)^2}{k(k+1)}$$

</details>

<details>
<summary>Чекор 2: Сумирање на левата страна (Телескопирање)</summary>

Целата лева страна на неравенката сега може да се запише како:

$$(\log_2 x)^2 \left( \frac{1}{1 \cdot 2} + \frac{1}{2 \cdot 3} + \dots + \frac{1}{2024 \cdot 2025} \right)$$

Користиме разложување на парцијални дропки $\frac{1}{k(k+1)} = \frac{1}{k} - \frac{1}{k+1}$:

$$(\log_2 x)^2 \left( \left( 1 - \frac{1}{2} \right) + \left( \frac{1}{2} - \frac{1}{3} \right) + \dots + \left( \frac{1}{2024} - \frac{1}{2025} \right) \right)$$

Сите средни членови се кратат (телескопираат), па останува само:

$$(\log_2 x)^2 \left( 1 - \frac{1}{2025} \right) = \frac{2024}{2025} (\log_2 x)^2$$

</details>

<details>
<summary>Чекор 3: Решавање на неравенката</summary>

Почетната неравенка сега станува:

$$\frac{2024}{2025} (\log_2 x)^2 \le 2024 \cdot \log_2 x$$

Делиме со 2024 и воведуваме смена $y = \log_2 x$:

$$\frac{y^2}{2025} \le y \implies y^2 - 2025y \le 0$$

Факторизираме:

$$y(y - 2025) \le 0$$

Решението на оваа квадратна неравенка е $0 \le y \le 2025$. Враќајќи ја смената:

$$0 \le \log_2 x \le 2025$$

Ова е еквивалентно на:

$$2^0 \le x \le 2^{2025} \implies 1 \le x \le 2^{2025}$$

Земајќи го предвид доменот ($x \ne 1$), конечното решение е:

$$1 < x \le 2^{2025}$$

</details>

**Краен одговор:** $\boxed{x \in (1, 2^{2025}]}$

## 👨‍🏫 Менторски Белешки
1.  **Златен Совет:** Телескопските суми се насекаде! Кога ќе видиш производ од два последователни броја или изрази во именител ($k \cdot (k+1)$), тоа е сигурен знак дека треба да ги разложиш на разлика од две дропки.
2.  **Чести Грешки:** Најчеста стапица е вклучувањето на бројот 1 во решението. Иако $y=0$ е решение на квадратната неравенка, во почетниот израз $x$ е основа на логаритам, па $x$ мора да биде строго поголем од 1 (бидејќи за $x=1$ логаритмите во именителот не се дефинирани).
3.  **Зошто ова е важно:** Овој тип задачи ги подготвува учениците за концептите на лимеси и редови во калкулусот, каде што разбирањето на сумирањето на големи низи е критично.

### 🔗 Поврзани вештини
* **Примарна вештина:** Логаритамски трансформации
* **Потребни предзнаења:** Својства на логаритми, решавање квадратни неравенки, телескопски суми.

# Manim Code
```python
from manim import *

class SolutionScene(Scene):
    def construct(self):
        self.camera.background_color = WHITE
        
        # --- CONFIGURATION ---
        # Main inequality
        ineq = MathTex(
            r"\sum_{k=1}^{2024} \frac{1}{\log_x 2^k \cdot \log_x 2^{k+1}} \le 2024 \log_2 x",
            color=BLACK
        ).shift(UP * 2)
        
        # Transformation step
        step1 = MathTex(
            r"\frac{(\log_2 x)^2}{k(k+1)}",
            color=BLUE
        ).next_to(ineq, DOWN, buff=0.5)
        
        # Telescoping result
        step2 = MathTex(
            r"(\log_2 x)^2 \cdot \frac{2024}{2025} \le 2024 \log_2 x",
            color=BLACK
        ).next_to(step1, DOWN, buff=0.5)
        
        # Final range
        final = MathTex(
            r"1 < x \le 2^{2025}",
            color=RED
        ).next_to(step2, DOWN, buff=0.8)

        # Draw box around answer
        box = SurroundingRectangle(final, color=BLACK, buff=0.2)

        # Animation (static frame for archive)
        self.add(ineq, step1, step2, final, box)
```