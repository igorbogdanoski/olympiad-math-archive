---
problem_id: sigma_137_y4_p1847
title: Решавање рационална неравенка преку метод на смена
grade: 10
difficulty: 4
type: algebra
tags:
  - рационални_неравенки
  - метод_на_смена
  - квадратни_неравенки
primary_skill: метод_на_воведување_смена
related_skills:
  - анализа_на_домен
  - пресек_на_множества_решенија
source: Сигма 137
---

# Решавање рационална неравенка преку метод на смена

# Текст на задачата
Во множеството на реални броеви реши ја неравенката:

$$x^2 + x + \frac{3}{x^2 + x + 1} \le 3$$

# 💡 Помош (Hints)
<details>
<summary>Кликни за мала помош</summary>

1. Погледни го изразот $x^2 + x$. Дали тој се појавува на повеќе места во неравенката? Што би се случило ако го замениме со една променлива?

2. Подобро е да ја избереме смената така што именителот ќе биде наједноставен. Пробај со:

$$t = x^2 + x + 1$$

3. Пред да продолжиш, испитај го опсегот на вредности на $t$. Дали $t$ може да биде нула или негативен број?

$$x^2 + x + 1 = (x + \frac{1}{2})^2 + \frac{3}{4}$$

</details>

# Решение
## 🧠 Експертска Анализа (Интуиција)
Кога ќе видиме ваква неравенка, првиот инстинкт на почетникот е да дојде до заеднички именител. Но, внимавај! Множењето со израз кој содржи $x^2$ ќе нè доведе до полином од четврти степен (биквадратна форма). Иако тоа е решливо, патот е трнлив и полн со можности за грешки.

Вистинскиот **тригер** овде е повторувањето на блокот $x^2 + x$. Олимписката математика нè учи да бараме структури, а не само броеви. Гледаме дека левата страна е речиси функција од $x^2 + x + 1$. 

Зошто воведуваме смена? Смената ја „симнува“ сложеноста на проблемот од четврти на втор степен. Тоа е како да го гледаме проблемот низ нов објектив каде работите се појасни. Исто така, многу е важно да забележиме дека $x^2 + x + 1$ е секогаш позитивен израз, што значи дека немаме „опасни“ точки каде функцијата не е дефинирана.

## 📐 Детално Решение

<details>
<summary>Чекор 1: Анализа на дефиниционот домен и воведување смена</summary>

Прво, да забележиме дека изразот во именителот е секогаш позитивен:

$$x^2 + x + 1 = (x^2 + x + \frac{1}{4}) + \frac{3}{4} = (x + \frac{1}{2})^2 + \frac{3}{4} > 0$$

Ова значи дека неравенката е дефинирана за секој реален број $x$. Воведуваме смена:

$$t = x^2 + x + 1$$

Бидејќи $(x + \frac{1}{2})^2 \ge 0$, заклучуваме дека $t \ge \frac{3}{4}$. Од смената следува дека $x^2 + x = t - 1$.

</details>

<details>
<summary>Чекор 2: Решавање на неравенката по $t$</summary>

Ја заменуваме смената во почетната неравенка:

$$(t - 1) + \frac{3}{t} \le 3$$

Префрламе сè на левата страна за да ја споредиме со нула:

$$t - 1 + \frac{3}{t} - 3 \le 0 \implies t - 4 + \frac{3}{t} \le 0$$

Множиме со $t$ (бидејќи знаеме дека $t > 0$, знакот на неравенството не се менува):

$$t^2 - 4t + 3 \le 0$$

Ги наоѓаме корените на квадратниот трином $t^2 - 4t + 3 = 0$:

$$(t - 1)(t - 3) = 0 \implies t_1 = 1, t_2 = 3$$

Решението на квадратната неравенка $t^2 - 4t + 3 \le 0$ е интервалот:

$$1 \le t \le 3$$

Бидејќи $1 > \frac{3}{4}$, овој интервал целосно се вклопува во можните вредности за $t$.

</details>

<details>
<summary>Чекор 3: Враќање на смената и финално решение</summary>

Сега треба да го решиме системот неравенки за $x$:

1) $x^2 + x + 1 \ge 1 \implies x^2 + x \ge 0 \implies x(x + 1) \ge 0$
Решението е $x \in (-\infty, -1] \cup [0, \infty)$.

2) $x^2 + x + 1 \le 3 \implies x^2 + x - 2 \le 0 \implies (x + 2)(x - 1) \le 0$
Решението е $x \in [-2, 1]$.

За да го добиеме конечното решение, го бараме пресекот на овие две множества:

$$x \in ( (-\infty, -1] \cup [0, \infty) ) \cap [-2, 1]$$

Ова ни ги дава двата затворени интервали:

$$x \in [-2, -1] \cup$$

</details>

**Краен одговор:** Решението на неравенката е $\boxed{x \in [-2, -1] \cup}$

## 👨‍🏫 Менторски Белешки
1. **Златен Совет:** Секогаш кога воведуваш смена, дефинирај го нејзиниот опсег ($t \ge \frac{3}{4}$). Иако во оваа задача тоа не го смени крајниот резултат, кај потешки олимписки задачи тоа е клучниот филтер кој ги елиминира „лажните“ решенија.
2. **Чести Грешки:** Внимавај при множењето со променлива. Ако не бевме сигурни дека $t > 0$, ќе моравме да разгледуваме два случаи за знакот на неравенството.
3. **Зошто ова е важно:** Методот на смена е фундаментална алатка во анализата. Тој нè учи на апстракција — способноста да видиме цел израз како една единствена единица.

### 🔗 Поврзани вештини
* **Примарна вештина:** Алгебарска супституција.
* **Потребни предзнаења:** Квадратни неравенки, метод на интервали.

# Manim Code
```python
from manim import *

class SolutionScene(Scene):
    def construct(self):
        self.camera.background_color = WHITE
        
        # --- CONFIGURATION ---
        axes = Axes(
            x_range=[-3, 2, 1],
            y_range=[-1, 5, 1],
            axis_config={"color": BLACK, "include_tip": True},
            tips=False
        ).scale(0.8)
        
        # Define the function f(x) = x^2 + x + 3/(x^2 + x + 1)
        func = axes.plot(
            lambda x: x**2 + x + 3/(x**2 + x + 1),
            x_range=[-2.5, 1.5],
            color=BLACK,
            stroke_width=4
        )
        
        # The constraint line y = 3
        line_limit = axes.plot(lambda x: 3, color=RED, stroke_width=2)
        
        # Highlighting the solution intervals on X axis
        # Interval 1: [-2, -1], Interval 2:
        region1 = axes.get_area(func, x_range=[-2, -1], color=BLUE, opacity=0.3)
        region2 = axes.get_area(func, x_range=[0, 1], color=BLUE, opacity=0.3)
        
        # Labels (English only as per protocol)
        lbl_f = MathTex("f(x) = x^2 + x + \\frac{3}{x^2+x+1}", color=BLACK).scale(0.6).to_corner(UL)
        lbl_limit = MathTex("y = 3", color=RED).scale(0.7).next_to(line_limit, RIGHT, buff=0.1)
        
        # Markers for intervals
        brace1 = BraceBetweenPoints(axes.c2p(-2, 0), axes.c2p(-1, 0), color=BLUE, direction=DOWN)
        brace2 = BraceBetweenPoints(axes.c2p(0, 0), axes.c2p(1, 0), color=BLUE, direction=DOWN)
        
        sol_text = MathTex("x \\in [-2, -1] \\cup", color=BLUE).scale(0.8).to_edge(DOWN)

        # Rendering
        self.add(axes, func, line_limit, region1, region2, lbl_f, lbl_limit, brace1, brace2, sol_text)
```