---
problem_id: sigma_137_y4_p1857
title: Магичен правоаголник со плочки (Корекција)
grade: 12
difficulty: 4
type: combinatorics
tags:
  - деливост
  - инваријанти
  - двојно_пребројување
primary_skill: метод_на_инваријанти
related_skills:
  - теорија_на_броеви
  - логичко_заклучување
source: Sigma 137
---

# Магичен правоаголник со плочки

# Текст на задачата
Дадени се 21 плочка во облик на квадратче со иста димензија. На четири плочки е запишан бројот 1, на две плочки е запишан бројот 2, на седум плочки е запишан бројот 3, а на осум плочки е запишан бројот 4. Користејќи 20 од тие плочки, Димитар формирал правоаголник со димензии $4 \times 5$. За формираниот правоаголник збирот на броевите во секоја редица е ист, и збирот на броевите во секоја колона е ист. Кој број стои на неискористената плочка?

# 💡 Помош (Hints)
<details>
<summary>Кликни за мала помош</summary>

1. Најпрво пресметај го вкупниот збир на сите 21 плочки. Тоа е почетната точка за секоја анализа на инваријанти.

$$S_{total} = \sum n_i v_i$$

2. Искористи го принципот на двојно пребројување. Ако збирот на секоја редица е $R$, а на секоја колона $C$, изрази го вкупниот збир $S$ преку димензиите 4 и 5.

$$S = 4R = 5C$$

3. Што ни кажува ова за деливоста на $S$ со бројот 20?

</details>

# Решение
## 🧠 Експертска Анализа (Интуиција)
Оваа задача е класичен пример за **инваријанти** во комбинаториката. Наместо да се обидуваме физички да ги подредиме плочките во правоаголникот, ние ја користиме „магичната“ особина на мрежата. Ако секоја редица има ист збир и секоја колона има ист збир, тогаш вкупниот збир на броевите мора да ги почитува аритметичките ограничувања наметнати од димензиите на мрежата.

Клучниот **тригер** е фразата „ист збир во секоја редица/колона“. Ова веднаш нè насочува кон тоа дека вкупниот збир $S$ мора да биде делив со бројот на редици (4) и бројот на колони (5). Бидејќи 4 и 5 се заемно прости, $S$ мора да биде делив со $4 \times 5 = 20$.

Грубата сила тука би значела обид за конструкција, што е губење време. Нашата „детективска работа“ се сведува на едноставна равенка: $S_{total} - x = S$, каде $x$ е вредноста на плочката што ја бараме, а $S$ е множител на 20.

## 📐 Детално Решение

<details>
<summary>Чекор 1: Пресметување на вкупниот збир</summary>

На располагање имаме 21 плочка со следните вредности:
- 4 плочки со вредност 1
- 2 плочки со вредност 2
- 7 плочки со вредност 3
- 8 плочки со вредност 4

Вкупниот збир на сите плочки $S_{total}$ е:

$$S_{total} = 4 \cdot 1 + 2 \cdot 2 + 7 \cdot 3 + 8 \cdot 4$$

$$S_{total} = 4 + 4 + 21 + 32 = 61$$

</details>

<details>
<summary>Чекор 2: Примена на условот за деливост</summary>

Нека $S$ е збирот на 20-те плочки искористени во правоаголникот $4 \times 5$. Нека $R$ е збирот на секоја редица, а $C$ збирот на секоја колона. Тогаш:

$$S = 4R \implies 4 \mid S$$

$$S = 5C \implies 5 \mid S$$

Од овие два услови, бидејќи $gcd(4, 5) = 1$, заклучуваме:

$$20 \mid S$$

</details>

<details>
<summary>Чекор 3: Изолирање на неискористената плочка</summary>

Збирот $S$ се разликува од $S_{total}$ за вредноста на една плочка $x \in \{1, 2, 3, 4\}$.

$$S = 61 - x$$

Бидејќи $S$ мора да биде делив со 20, ги проверуваме можните вредности:
- Ако $x=1$, тогаш $S=60$ (Деливо со 20, $60 = 20 \times 3$).
- Ако $x=2$, тогаш $S=59$ (Не е деливо).
- Ако $x=3$, тогаш $S=58$ (Не е деливо).
- Ако $x=4$, тогаш $S=57$ (Не е деливо).

Следниот содржател на 20 е 40, но тогаш би ни требало $x = 61 - 40 = 21$, што не е можно. Единствено решение е $x=1$.

</details>

**Краен одговор:** $\boxed{1}$

## 👨‍🏫 Менторски Белешки
1. **Златен Совет:** При решавање задачи со мрежи и збирови, секогаш користи го „двојното пребројување“. Тоа е најстариот трик во книгата кој никогаш не изневерува.
2. **Чести Грешки:** Некои студенти забораваат да го проверат опсегот на $x$. Можеби $S=40$ изгледа привлечно, но во олимписката математика секогаш проверувај дали твоите „остатоци“ (во овој случај $x=21$) се во рамките на дадените услови.
3. **Зошто ова е важно:** Овој концепт на инваријанти е темел на многу посложени теореми во комбинаторната геометрија и теоријата на графови.

### 🔗 Поврзани вештини
* **Примарна вештина:** Метод на инваријанти
* **Потребни предзнаења:** Својства на деливост, аритметика на модули.

# Manim Code
```python
from manim import *

class SolutionScene(Scene):
    def construct(self):
        self.camera.background_color = WHITE

        # --- Draw 4x5 grid of squares ---
        # Fixed logic: Using a more robust VGroup and cleaner alignment
        rows, cols = 4, 5
        square_size = 0.8
        grid = VGroup()
        
        for i in range(rows):
            for j in range(cols):
                sq = Square(side_length=square_size, color=BLACK, stroke_width=3)
                # Position relative to center
                x_pos = (j - (cols - 1) / 2) * square_size
                y_pos = (i - (rows - 1) / 2) * square_size
                sq.move_to([x_pos, -y_pos, 0]) # Negative y to draw top-to-bottom
                grid.add(sq)
        
        grid.scale(0.8).shift(LEFT * 1.5)
        self.add(grid)

        # Labels - Using English as per rules
        row_label = MathTex("4 \\text{ Rows}", color=BLACK).scale(0.8).next_to(grid, LEFT, buff=0.5)
        col_label = MathTex("5 \\text{ Columns}", color=BLACK).scale(0.8).next_to(grid, DOWN, buff=0.5)
        self.add(row_label, col_label)

        # Derivations - Logic block
        deriv_block = VGroup(
            MathTex("S = 4 \\times R", color=BLACK),
            MathTex("S = 5 \\times C", color=BLACK),
            MathTex("\\implies 20 | S", color=BLUE)
        ).arrange(DOWN, aligned_edge=LEFT, buff=0.3).to_corner(UR, buff=1)
        
        # Calculation block
        calc_block = VGroup(
            MathTex("Total = 61", color=BLACK),
            MathTex("61 - 1 = 60", color=BLACK),
            MathTex("x = 1", color=RED)
        ).arrange(DOWN, aligned_edge=LEFT, buff=0.3).next_to(deriv_block, DOWN, buff=1)
        
        box = SurroundingRectangle(calc_block, color=RED, buff=0.1)
        
        self.add(deriv_block, calc_block, box)
```