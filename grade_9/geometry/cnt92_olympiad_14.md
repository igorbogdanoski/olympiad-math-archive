---
grade: 9
field: geometry
difficulty: 4
problem_type: calculation
source: "<натпревар / списание / година>"
problem_id: 14
language_original: <mk | en | sr | hr | ...>
translated: false

# --- GEOMETRY SKILLS ---
geometry_style: analytic
primary_skill: <main_tool>
related_skills:
  - linear_inequalities
  - approximation
prerequisites:
  - linear_functions
  - radicals

# --- VISUALIZATION ---
visual_prompt: "No visual prompt provided."

tags:
  - geometry
  - olympiad
---

# Позиција на точки во однос на права

## Текст на задачата
Точките $A(1; 3)$ и $B(2; 5)$ се распоредени во однос на правата $y = \sqrt{2}x + \sqrt{3}$ на одреден начин. Одреди дали се над или под правата.

## 📐 Скица / Конструкција
<!-- VISUAL PROMPT: No visual prompt provided. -->


> **👨‍💻 Manim Code (за Geo-Mentor):**
> ```python
> class Task_14(Scene):
>     def construct(self):
>         self.camera.background_color = WHITE
>         # ... (копирај од логот) ...
> ```

## 🧠 Анализа
За да одредиме дали точка $P(x_0, y_0)$ е над или под правата $y = kx + n$, треба да ја споредиме $y$-координатата на точката со вредноста на функцијата во таа $x$-координата. Ако $y_0 > f(x_0)$, точката е над правата. Предизвикот овде е ирационалноста, па користете апроксимации $\sqrt{2} \approx 1.41$ и $\sqrt{3} \approx 1.73$.

## 📝 Решение (СИНТЕТИЧКО)
Равенката на правата е $f(x) = \sqrt{2}x + \sqrt{3}$.

**Чекор 1: Проверка за точката $A(1; 3)$**
Заменуваме $x=1$ во равенката:
$$ f(1) = \sqrt{2}(1) + \sqrt{3} \approx 1.414 + 1.732 = 3.146 $$
Бидејќи $y_A = 3$, а $3 < 3.146$, заклучуваме дека $y_A < f(1)$.
$\Rightarrow$ Точката $A$ е **под** правата.

**Чекор 2: Проверка за точката $B(2; 5)$**
Заменуваме $x=2$ во равенката:
$$ f(2) = \sqrt{2}(2) + \sqrt{3} \approx 2(1.414) + 1.732 = 2.828 + 1.732 = 4.56 $$
Бидејќи $y_B = 5$, а $5 > 4.56$, заклучуваме дека $y_B > f(2)$.
$\Rightarrow$ Точката $B$ е **над** правата.

**Заклучок:** $A$ е долу, $B$ е горе.

## 🏁 Заклучок
<Краен резултат.>

## 👩‍🏫 За наставници
Оваа задача ги учи учениците на геометриското значење на неравенките. Не е потребно цртање 'од око', туку алгебарска проверка.